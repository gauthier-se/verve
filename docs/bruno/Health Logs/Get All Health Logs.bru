meta {
  name: Get All Health Logs
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/logs?userId=1
  body: none
  auth: none
}

params:query {
  userId: 1
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response should have success", function() {
    expect(res.getBody()).to.have.property('success', true);
  });
  
  test("Response should have data array", function() {
    expect(res.getBody().data).to.be.an('array');
  });
}
